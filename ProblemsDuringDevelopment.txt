==2026-08-19==
"Administrator(s)" role not working even if the login I used was assigned the role's ID

==2026-08-20==
* Application failing to load data from the database because of a JsonReaderException
* "Edit" View for both Media subclasses showing System.String[] instead of the different actors' names and the link to reviews/comments (Asked Copilot what was necessary for this and past points)
* Toggling the administrator interface to show either the usual table, or the visitors'


==2026-08-21==
* Setting up API for fetching movies and TV shows from for instance, SVT Play. It requires an API key for authorization, requiring I email them and get approved before I can fetch from them.
* 
* 
* 


==2026-08-24==
* 
* 
* 
* 

==2026-08-25==
* Adding respective strings represented by bits (with Flags) is still difficult
* API will need to wait before more is done with it